# 部署手册（Vite + Vue）

本项目是 **Vite 构建的纯前端静态站点**，生产环境只需要部署构建产物 `dist/` 到任意静态文件服务器（Nginx、Apache、对象存储、CDN 等）。

---

## 1. 环境准备

- Node.js：建议 **18+ / 20+**
- 包管理器：npm（本项目已包含 `package-lock.json`）

---

## 2. 本地构建与自检

在项目根目录执行：

```bash
npm install
npm run build
npm run preview
```

- 构建输出：`dist/`
- 预览地址：终端会打印 `http://localhost:4173/`（Vite preview 默认端口可能为 4173）

---

## 3. 服务器部署（推荐：Nginx）

### 3.1 上传构建产物

在本地构建后，把整个 `dist/` 上传到服务器，例如：

- 服务器目录：`/var/www/eduleaderboard/`（目录内直接放 `index.html`、`assets/` 等）

### 3.2 Nginx 配置（SPA 刷新不 404）

创建或修改 Nginx 站点配置（示例）：

```nginx
server {
  listen 80;
  server_name your-domain.com; # 或服务器 IP

  root /var/www/eduleaderboard;
  index index.html;

  # 关键：单页应用路由回退
  location / {
    try_files $uri $uri/ /index.html;
  }

  # 静态资源缓存（可选）
  location /assets/ {
    expires 30d;
    add_header Cache-Control "public, max-age=2592000, immutable";
    try_files $uri =404;
  }
}
```

重载 Nginx：

```bash
sudo nginx -t
sudo systemctl reload nginx
```

---

## 4. Docker 部署（Nginx 容器直接托管 dist）

### 4.1 方案 A：本地构建后打包 dist（最简单）

1) 本地构建：

```bash
npm ci
npm run build
```

2) 在服务器（或本地）准备一个临时目录，放入 `dist/`，并创建 `nginx.conf`：

```nginx
server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;
  location / {
    try_files $uri $uri/ /index.html;
  }
}
```

3) 运行容器（将 dist 挂载进去）：

```bash
docker run -d --name eduleaderboard -p 80:80 \
  -v "$(pwd)/dist:/usr/share/nginx/html:ro" \
  -v "$(pwd)/nginx.conf:/etc/nginx/conf.d/default.conf:ro" \
  nginx:alpine
```

---

## 5. 部署到子路径（重要：Vite `base`）

如果你不是部署在域名根路径 `/`，而是类似：

- `https://example.com/eduleaderboard/`

则需要把 Vite 的 `base` 设置为子路径，否则静态资源会 404。

修改 `vite.config.mts`：

```ts
export default defineConfig({
  base: "/eduleaderboard/",
  plugins: [vue()],
  server: { port: 5173, host: "0.0.0.0" }
});
```

然后重新构建并部署 `dist/`。


